export default function ReportsPage() {
  return (
    <section className="card max-w-2xl p-7">
      <p className="text-sm font-semibold text-brand-600">Future phase placeholder</p>
      <h1 className="mt-2 text-2xl font-semibold">Reports</h1>
      <p className="mt-3 text-sm leading-relaxed text-slate-500">Reports, conversion analysis, source performance and CSV export are intentionally reserved for a later phase. Phase 1 focuses on authenticated lead capture and assignment.</p>
    </section>
  );
}
